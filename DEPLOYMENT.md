#!/bin/bash
set -e

echo "=========================================="
echo "Data Cleaning Environment - Deployment Guide"
echo "=========================================="
echo ""

REPO_URL="https://github.com/redhatsam09/scaler-ml"
SPACE_NAME="data-cleaning-env"
HF_USERNAME="redhatsam09"
HF_SPACE_URL="https://huggingface.co/spaces/${HF_USERNAME}/${SPACE_NAME}"

echo "STEP 1: Push to GitHub (if not done)"
echo "=========================================="
echo "$ cd /workspaces/scaler-ml"
echo "$ git add ."
echo "$ git commit -m 'OpenEnv data cleaning environment'"
echo "$ git push origin main"
echo ""

echo "STEP 2: Create Hugging Face Space"
echo "=========================================="
echo "Go to: https://huggingface.co/new-space"
echo ""
echo "Settings:"
echo "  Space name: ${SPACE_NAME}"
echo "  License: OpenRAIL"
echo "  Space SDK: Docker"
echo "  Visibility: Public"
echo "  Hardware: CPU Basic (2vCPU, 8GB RAM)"
echo ""

echo "STEP 3: Configure Environment Variables"
echo "=========================================="
echo "In Space Settings → Variables:"
echo "  API_BASE_URL=https://api.openai.com/v1"
echo "  MODEL_NAME=gpt-4"
echo "  HF_TOKEN=hf_your_huggingface_token"
echo "  OPENAI_API_KEY=sk_your_openai_key"
echo ""

echo "STEP 4: Wait for Space Deployment"
echo "=========================================="
echo "Space URL: ${HF_SPACE_URL}"
echo "Status: Check 'App Status' on the Space page"
echo "Once green: Space is live and ready"
echo ""

echo "STEP 5: Test Your Space"
echo "=========================================="
echo "$ curl -X POST https://${HF_USERNAME}-${SPACE_NAME}.hf.space/reset -d '{}'"
echo "Should return observation object with dataset info"
echo ""

echo "STEP 6: Submit to Competition"
echo "=========================================="
echo "Fill submission form with:"
echo "  HF Space URL: https://${HF_USERNAME}-${SPACE_NAME}.hf.space"
echo ""

echo "=========================================="
echo "Pre-Submission Checklist"
echo "=========================================="
echo "✓ openenv validate passes"
echo "✓ Docker builds successfully"
echo "✓ Server /reset returns 200"
echo "✓ 3+ tasks with graders"
echo "✓ Baseline inference script (inference.py)"
echo "✓ README with setup instructions"
echo "✓ All typed models (Pydantic)"
echo "✓ Real-world task (data cleaning)"
echo ""
